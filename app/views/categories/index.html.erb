<!DOCTYPE html>
<html>
  <head>
    <title>Category Buttons</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <link href="https://stackpath.bootstrapcdn.com/bootstrap/5.1.3/css/bootstrap.min.css" rel="stylesheet">

    <style>
      .btn-category {
        font-size: 1.25rem; /* Increase button font size */
        padding: 1rem; /* Increase button padding */
      }
      .mt-5 {
        margin-top: 3rem !important; /* Increase margin top for container */
      }
      .mb-5 {
        margin-bottom: 3rem !important; /* Increase margin bottom for title */
      }
      .mt-3 {
        margin-top: 1rem !important; /* Increase margin top for row */
      }
    </style>
  </head>
  <body>
    <div class="container mt-5">
      <h1 class="text-center mb-5">Where do you want to eat in <%= @chosen_city %>?</h1>
      <div class="row mt-3">
        <% @list_of_categories.each do |category| %>
          <div class="col-md-3 mb-4">
            <a href="/<%= @chosen_city %>/<%= category.name %>" class="btn btn-primary btn-block btn-category"><%= category.name %></a>
          </div>
        <% end %>
      </div>
    </div>

    <script src="https://code.jquery.com/jquery-3.6.0.min.js"></script>
    <script src="https://stackpath.bootstrapcdn.com/bootstrap/5.1.3/js/bootstrap.bundle.min.js"></script>
  </body>
</html>

